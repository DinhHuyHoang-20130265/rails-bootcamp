<h1>Hi, <%= current_user.display_name || "User" %> @<%=current_user.username %></h1>

<p>You are logged in ✅</p>

<%= button_to "Logout", destroy_user_session_path,
              method: :delete,
              data: { turbo: false } %>
